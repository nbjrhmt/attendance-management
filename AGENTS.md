# 项目说明

## 项目名称
乡村基层活动智能签到管理平台

## 项目概述
这是一个面向乡村基层的活动签到管理系统，支持家庭为单位进行人脸签到，
管理员可以创建签到活动、查看签到统计报表。

## 技术栈
- 后端：Python 3.11 + FastAPI + SQLAlchemy + PyMySQL + Redis
- 数据库：MySQL 8.0
- 缓存：Redis
- 认证：JWT（python-jose + passlib）
- 前端管理后台：Vue 3 + Element Plus（后期开发）
- 家庭端：微信小程序（后期开发）
- 人脸识别：百度AI人脸识别API（后期接入）

## 项目结构
backend/
├── src/
│ ├── auth/ # 登录注册、JWT认证、权限
│ ├── user/ # 用户管理
│ ├── family/ # 家庭管理
│ ├── member/ # 家庭成员管理
│ ├── face/ # 人脸录入、识别
│ ├── event/ # 签到活动管理
│ ├── checkin/ # 签到业务
│ ├── leave/ # 请假管理
│ ├── statistics/ # 统计报表
│ ├── log/ # 操作日志
│ ├── common/ # 公共工具（数据库连接、响应格式、基础模型）
│ └── config/ # 配置（数据库、Redis、JWT密钥等）
├── tests/
├── docs/
├── main.py # 应用入口
├── requirements.txt # 依赖清单
└── README.md


## 角色说明
- **admin（管理员）**：可以创建活动、管理所有数据、查看统计报表、审批请假
- **staff（工作人员）**：可以协助管理员进行签到操作
- **family（家庭用户）**：可以注册、管理家庭成员、进行人脸签到

## 核心业务规则
- 家庭以户主为单位注册，户主可以添加/编辑/删除家庭成员
- 每个家庭成员可以选择是否需要签到
- 签到活动由管理员创建，指定时间范围和迟到阈值
- 签到方式：人脸识别签到（主要）、手动签到（备用）
- 请假需要提前申请，管理员审批
- 签到状态：已签到(signed)、迟到(late)、缺勤(absent)、请假(leave)、异常(abnormal)

## 接口规范
- 所有接口使用 RESTful 风格
- 接口路径统一以 /api/ 开头
- 所有接口返回统一格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}