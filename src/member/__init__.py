"""家庭成员管理模块：户主可添加、编辑、删除家庭成员，并设置是否需要签到。

已实现（阶段三）：

- ``POST   /api/members``：添加家庭成员 —— 户主本人/管理员（管理员需指定 family_id）
- ``GET    /api/members``：成员列表（分页、状态/是否需签到筛选、关键字搜索）—— 户主/管理员/工作人员
- ``GET    /api/members/{member_id}``：成员详情 —— 户主本人/管理员/工作人员
- ``PUT    /api/members/{member_id}``：修改成员信息 —— 户主本人/管理员
- ``PUT    /api/members/{member_id}/status``：启用/停用成员 —— 户主本人/管理员
- ``DELETE /api/members/{member_id}``：删除成员 —— 户主本人/管理员

成员字段：姓名、性别、与户主关系、身份证号（唯一，含校验位校验）、出生日期、
联系电话、是否需要签到、状态、备注。填写身份证号时，性别与出生日期可留空自动识别。

户主本人也是一名家庭成员：注册建档时会自动生成 ``relation=householder`` 的成员记录，
并关联到登录账号（``user_id``）。

模块结构：

- ``models``：``family_member`` 表与性别/关系/状态枚举
- ``id_card``：身份证号校验与出生日期、性别解析
- ``schemas``：请求/响应数据结构
- ``crud``：数据访问层
- ``service``：业务逻辑与权限规则
- ``router``：接口层
"""
