# ======================================================================
# 乡村基层活动智能签到管理平台 —— 后端依赖清单
# 适用环境：Python 3.11+（当前虚拟环境 Python 3.13.9 已验证通过）
# 安装命令：pip install -r requirements.txt
# ======================================================================

# ------------------------------ Web 框架 ------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ------------------------------ 数据库 --------------------------------
SQLAlchemy>=2.0.30
PyMySQL>=1.1.1
# MySQL 8 默认使用 caching_sha2_password 认证插件，PyMySQL 需要 cryptography
cryptography>=42.0.0

# ------------------------------ 缓存 ----------------------------------
redis>=5.0.0

# --------------------------- 配置与数据校验 ----------------------------
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# ---------------------------- 认证与安全 -------------------------------
python-jose[cryptography]>=3.4.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 读取 bcrypt.__about__.__version__，bcrypt>=4.1 已移除该属性会打印告警，故固定版本
bcrypt==4.0.1

# ------------------ 表单与文件上传（人脸照片上传等） -------------------
python-multipart>=0.0.9

# ------------------------ 外部服务与测试客户端 -------------------------
# 调用百度 AI 人脸识别接口（阶段四）；TestClient 亦依赖 httpx
httpx>=0.27.0

# ------------------------ 开发/测试依赖（可选） ------------------------
pytest>=8.2.0
